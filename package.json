{
  "name": "lookmd",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted, workspace-scoped Markdown viewer and editor.",
  "license": "MIT",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  }
}
